package nextstep.courses.domain;

public class Session {
    private final DateRange sessionDateRange;

    public Session(final DateRange sessionDateRange) {
        this.sessionDateRange = sessionDateRange;
    }
}
